# Phase 2 — GNN Scoring Dependencies
# Install in this order to avoid conflicts.

# ── Core ML ──────────────────────────────────────────────────────────
# PyTorch: install the CPU build (replace cu118/cu121 for GPU)
# Windows: https://pytorch.org/get-started/locally/
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# PyTorch Geometric (installs torch-geometric + its sparse/scatter wheels)
# pip install torch-geometric
# Optional scatter/sparse extensions (needed for SAGEConv, HGTConv):
# pip install torch-scatter torch-sparse -f https://data.pyg.org/whl/torch-2.4.0+cpu.html

# ── Class imbalance ──────────────────────────────────────────────────
imbalanced-learn>=0.12.0

# ── Ensemble classifiers ─────────────────────────────────────────────
xgboost>=2.0.0
lightgbm>=4.3.0
scikit-learn>=1.4.0

# ── Data & utilities ─────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
tqdm>=4.66.0
matplotlib>=3.8.0
joblib>=1.3.0

# ── Graph DB ─────────────────────────────────────────────────────────
neo4j>=5.18.0
python-dotenv>=1.0.0
