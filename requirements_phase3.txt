# Phase 3 — GraphRAG + LangGraph Dependencies

# ── LLM ──────────────────────────────────────────────────────────────
openai>=1.30.0             # OpenRouter uses OpenAI-compatible API

# ── Orchestration ────────────────────────────────────────────────────
langgraph>=0.2.28          # Agentic pipeline StateGraph
langchain-core>=0.3.0      # Shared abstractions (Messages, prompts)

# ── Embeddings ───────────────────────────────────────────────────────
sentence-transformers>=3.0.0   # Local embeddings (all-MiniLM-L6-v2, 384-dim)
torch>=2.0.0                   # sentence-transformers dependency

# ── Vector store ─────────────────────────────────────────────────────
scikit-learn>=1.4.0        # cosine_similarity for local vector store
numpy>=1.26.0

# Optional: Pinecone (set VECTOR_STORE_BACKEND=pinecone in .env)
# pinecone-client>=3.0.0

# ── Data & utilities ─────────────────────────────────────────────────
pandas>=2.1.0
tqdm>=4.66.0
python-dotenv>=1.0.0
joblib>=1.3.0

# ── Graph DB ─────────────────────────────────────────────────────────
neo4j>=5.18.0
